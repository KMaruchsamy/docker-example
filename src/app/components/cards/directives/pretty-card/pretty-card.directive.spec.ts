import { PrettyCardDirective } from "./pretty-card.directive";


describe('PrettyCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PrettyCardDirective();
    expect(directive).toBeTruthy();
  });
});
