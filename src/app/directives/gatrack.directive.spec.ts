import { GatrackDirective } from './gatrack.directive';

describe('GatrackDirective', () => {
  it('should create an instance', () => {
    const directive = new GatrackDirective();
    expect(directive).toBeTruthy();
  });
});
